\documentclass[
  oneside,
  openany,
  titlepage,
  11pt,
  a4paper,
  italian,
  DIV=14,
  headinclude,
  footinclude=true,
  cleardoublepage=empty
]{scrbook}

% --- ClassicThesis + ArsClassica (Pantieri) ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{babel}
\usepackage{lmodern}
\usepackage{mathpazo}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{mathrsfs}
\usepackage[
  eulerchapternumbers,
  beramono,
  eulermath,
  pdfspacing,
  parts
]{classicthesis}
\usepackage{arsclassica}

% --- nota di colore vinaccia per il numero del capitolo ---
\definecolor{halfgray}{rgb}{0.55,0.13,0.16}
\definecolor{cream}{RGB}{245,240,228}
\definecolor{terra}{rgb}{0.42,0.26,0.13}  % terra d'ombra calda, per stratificazioni semi-trasparenti

% --- aumenta lo spazio dopo il titolo del capitolo ---
\usepackage{titlesec}
\titlespacing*{\chapter}{0pt}{2.5\baselineskip}{4\baselineskip}

% --- lingua e tolleranze ---
\selectlanguage{italian}
\emergencystretch=3em
\tolerance=2000
\hbadness=2000

% --- grafica ---
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,decorations.pathmorphing,calc}

% --- epigrafi (per attacchi capitolo, opzionale) ---
\usepackage{epigraph}
\setlength{\epigraphwidth}{0.55\textwidth}
\renewcommand{\epigraphflush}{flushright}
\renewcommand{\textflush}{flushright}
\renewcommand{\epigraphsize}{\small\itshape}

% --- regole di inferenza in stile naturale ---
\usepackage{mathpartir}

% --- codice Agda con listings (no shell-escape richiesto) ---
\usepackage{listings}
\usepackage{xcolor}
\definecolor{agdaKw}{rgb}{0.55,0.13,0.16}   % maroon, come halfgray
\definecolor{agdaCom}{rgb}{0.45,0.45,0.45}
\definecolor{agdaStr}{rgb}{0.20,0.40,0.20}

% --- mapping unicode per Agda (così pdflatex digerisce i simboli) ---
\lstdefinelanguage{Agda}{
  keywords={data, where, with, module, open, import, public, private,
            renaming, hiding, using, let, in, do, postulate, abstract,
            primitive, mutual, infix, infixl, infixr, syntax, pattern,
            instance, record, field, constructor, Set, Type, Prop,
            forall, λ, lambda},
  sensitive=true,
  comment=[l]{--},
  morecomment=[s]{\{-}{-\}},
  string=[b]",
  basicstyle=\ttfamily\small,
  keywordstyle=\color{agdaKw}\bfseries,
  commentstyle=\color{agdaCom}\itshape,
  stringstyle=\color{agdaStr},
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  literate=
    {𝔹}{{$\mathbb{B}$}}1
    {ℕ}{{$\mathbb{N}$}}1
    {≡}{{$\equiv$}}1
    {∀}{{$\forall$}}1
    {→}{{$\rightarrow$}}1
    {⊢}{{$\vdash$}}1
    {⊥}{{$\bot$}}1
    {⊤}{{$\top$}}1
    {⟦}{{$\llbracket$}}1
    {⟧}{{$\rrbracket$}}1
    {λ}{{$\lambda$}}1
    {Π}{{$\Pi$}}1
    {Σ}{{$\Sigma$}}1
    {α}{{$\alpha$}}1
    {β}{{$\beta$}}1
    {δ}{{$\delta$}}1
    {ε}{{$\varepsilon$}}1
    {ρ}{{$\rho$}}1
    {τ}{{$\tau$}}1
    {φ}{{$\varphi$}}1
    {₁}{{$_1$}}1
    {₂}{{$_2$}}1
    {⟨}{{$\langle$}}1
    {⟩}{{$\rangle$}}1
    {≢}{{$\not\equiv$}}1
    {∎}{{$\blacksquare$}}1
    {∷}{{$::$}}1
    {↑}{{$\uparrow$}}1
    {↓}{{$\downarrow$}}1
    {ℝ}{{$\mathbb{R}$}}1
    {ℤ}{{$\mathbb{Z}$}}1
    {⊎}{{$\uplus$}}1
    {★}{{$\star$}}1
    {∧}{{$\wedge$}}1
    {∨}{{$\vee$}}1
    {¬}{{$\neg$}}1
    {∃}{{$\exists$}}1
    {∘}{{$\circ$}}1
    {×}{{$\times$}}1
    {è}{{\`e}}1
    {à}{{\`a}}1
    {ò}{{\`o}}1
    {ì}{{\`\i }}1
    {ù}{{\`u}}1
    {é}{{\'e}}1
    {È}{{\`E}}1
    {À}{{\`A}}1
    {Ì}{{\`I}}1
    {Ò}{{\`O}}1
    {Ù}{{\`U}}1
    {É}{{\'E}}1,
}

\lstnewenvironment{agda}[1][]{\lstset{language=Agda,#1}}{}
\newcommand{\agdainl}[1]{\lstinline[language=Agda,basicstyle=\ttfamily\normalsize]{#1}}

% --- ambienti teorema-like in stile ClassicThesis (identici al sorgente avit-io) ---
\newtheoremstyle{ctplain}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\itshape}{}%
  {\bfseries\scshape}{.}%
  {.5em}{}%
\newtheoremstyle{ctdef}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\normalfont}{}%
  {\bfseries\scshape}{.}%
  {.5em}{}%
\newtheoremstyle{ctrem}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\normalfont}{}%
  {\itshape}{.}%
  {.5em}{}%

% --- ambiente per i ceri: stesso stile sobrio dello stile def, ma con head dedicato ---
\newtheoremstyle{ctcero}%
  {0.9\baselineskip}{0.7\baselineskip}%
  {\normalfont}{}%
  {\bfseries\scshape\color{halfgray}}{.}%
  {.5em}{}%

\theoremstyle{ctplain}
\newtheorem{regola}{Regola}[chapter]

\theoremstyle{ctdef}
\newtheorem{defn}[regola]{Definizione}

\theoremstyle{ctrem}
\newtheorem*{oss}{Osservazione}

\theoremstyle{ctcero}
\newtheorem{cero}{Cero}

% --- comandi locali ---
\newcommand{\meta}[1]{\textit{#1}}        % per il metalinguaggio in prosa
\newcommand{\lang}[1]{\textbf{#1}}        % per il linguaggio in prosa
\newcommand{\definitorio}{\ensuremath{\equiv_{\beta}}}
\newcommand{\identita}{\ensuremath{\equiv}}
\newcommand{\Type}{\mathsf{Type}}
\newcommand{\Set}{\mathsf{Set}}
\newcommand{\indB}{\mathsf{ind\text{-}\mathbb{B}}}
\newcommand{\indN}{\mathsf{ind\text{-}\mathbb{N}}}
\newcommand{\indS}{\mathsf{ind\text{-}\Sigma}}
\newcommand{\indV}{\mathsf{ind\text{-}Vec}}
\newcommand{\Jelim}{\mathsf{J}}
\newcommand{\refl}{\mathsf{refl}}
\newcommand{\zero}{\mathsf{zero}}
\newcommand{\suc}{\mathsf{suc}}
\newcommand{\tto}{\mathsf{tt}}
\newcommand{\ff}{\mathsf{ff}}
\newcommand{\nil}{\mathsf{nil}}
\newcommand{\cons}{\mathsf{cons}}
\newcommand{\andd}{\mathbin{\&\&}}

% --- hyperref ---
\hypersetup{%
  colorlinks=true,
  linkcolor=Maroon,
  citecolor=Maroon,
  urlcolor=Maroon,
  pdftitle={Prova o taci},
  pdfsubject={appunti di MLTT per Verified Functional Programming in Agda},
  pdfauthor={}
}

% --- TOC ---
\usepackage{titletoc}
\renewcommand{\cftpartfont}{\color{CTtitle}}
\renewcommand{\cftpartaftersnum}{}
\renewcommand{\cftpartaftersnumb}{}
\renewcommand{\cftchapfont}{}
\renewcommand{\cftchappresnum}{}
\renewcommand{\cftchapaftersnumb}{}

\begin{document}
\frenchspacing
\raggedbottom
\selectlanguage{italian}
\renewcommand{\partname}{Parte}
\renewcommand{\chaptername}{Capitolo}
\renewcommand{\contentsname}{Indice}

\pagenumbering{roman}
\pagestyle{plain}

% ----------------- frontespizio (Depero / Balla / Boccioni — la regola come evento) -----------------
\begin{titlepage}
\thispagestyle{empty}
\begin{tikzpicture}[remember picture, overlay]

  %% =========================================================
  %% SFONDO MATERICO --- cream con macchie irregolari, niente bande
  %% =========================================================
  \fill[cream] (current page.north west) rectangle (current page.south east);

  %% Macchie semi-trasparenti, di terra d'ombra e vinaccia, sparse e ruotate
  \foreach \x/\y/\rx/\ry/\rot/\col/\op in {%
    3.2/-2.5/3.4/2.1/12/terra/0.22,
    17/-6/3.8/2.8/-22/terra/0.18,
    1.5/-9/2.0/2.6/35/halfgray/0.16,
    14/-15/2.5/3.5/-8/terra/0.15,
    7/-18/3.1/2.0/55/halfgray/0.13,
    4/-22/2.7/1.9/25/terra/0.16,
    18/-23/2.2/2.8/40/halfgray/0.15,
    9/-25/3.0/1.7/-40/terra/0.14,
    2/-27/2.0/1.8/30/terra/0.18%
  } {
    \fill[\col, opacity=\op, rotate around={\rot:($(current page.north west)+(\x cm,\y cm)$)}]
      ($(current page.north west)+(\x cm,\y cm)$) ellipse[x radius=\rx cm, y radius=\ry cm];
  }

  %% Nuclei densi di pigmento, piccoli punti d'accento
  \foreach \x/\y/\r/\col/\op in {%
    4/-4/0.30/terra/0.40,
    11/-7/0.22/halfgray/0.45,
    17/-12/0.18/halfgray/0.50,
    9/-20/0.25/terra/0.35,
    13/-26/0.20/halfgray/0.45%
  } {
    \fill[\col, opacity=\op]
      ($(current page.north west)+(\x cm,\y cm)$) circle[radius=\r cm];
  }

  %% =========================================================
  %% LINEE DI FORZA BALLA --- fasci direzionali, tagli netti
  %% =========================================================

  %% Fascio principale dall'angolo alto-destra (vinaccia)
  \foreach \a in {195,210,225,240,255} {
    \draw[halfgray, line width=0.4pt, opacity=0.32, -{Stealth[length=2pt]}]
      ($(current page.north east)+(-1,-3)$) -- ++(\a:15cm);
  }

  %% Fascio contrario dall'alto-sinistra (nero)
  \foreach \a in {-15,-30,-45,-60} {
    \draw[black, line width=0.32pt, opacity=0.20, -{Stealth[length=2pt]}]
      ($(current page.north west)+(1,-2)$) -- ++(\a:13cm);
  }

  %% =========================================================
  %% CURVA PLASTICA BOCCIONI --- forma unica di continuità nello spazio
  %% =========================================================

  %% Curva principale: spessa, vinaccia, sweep diagonale
  \draw[halfgray, line width=2.6pt, opacity=0.55]
    ($(current page.north west)+(-1,-9)$)
    .. controls
       ($(current page.north west)+(8,-12)$) and
       ($(current page.north west)+(11,-18)$)
    .. ($(current page.north east)+(2,-21)$);

  %% Eco posteriore, sottile e in nero (traccia che insegue la forma)
  \draw[black, line width=1.1pt, opacity=0.38]
    ($(current page.north west)+(-1,-9.8)$)
    .. controls
       ($(current page.north west)+(8,-12.8)$) and
       ($(current page.north west)+(11,-18.8)$)
    .. ($(current page.north east)+(2,-21.8)$);

  %% =========================================================
  %% TITOLO --- asimmetrico, ruotato, GRIDATO con echi sfasati
  %% =========================================================

  %% Eco vinaccia, ruotata più in basso
  \node[halfgray, opacity=0.42, font=\fontsize{52}{56}\selectfont\bfseries\scshape,
        rotate=-11, anchor=west]
    at ($(current page.north west)+(0.5,-5.4)$)
    {\spacedlowsmallcaps{Prova o taci}};

  %% Eco terra, ruotata in alto
  \node[terra, opacity=0.45, font=\fontsize{52}{56}\selectfont\bfseries\scshape,
        rotate=-4, anchor=west]
    at ($(current page.north west)+(1.1,-5.7)$)
    {\spacedlowsmallcaps{Prova o taci}};

  %% Titolo in primo piano
  \node[black, font=\fontsize{52}{56}\selectfont\bfseries\scshape,
        rotate=-7, anchor=west]
    at ($(current page.north west)+(0.7,-5.1)$)
    {\spacedlowsmallcaps{Prova o taci}};

  %% Filo vinaccia decorativo, anche lui ruotato
  \draw[halfgray, line width=2.2pt,
        rotate around={-7:($(current page.north west)+(0.7,-6.4)$)}]
    ($(current page.north west)+(0.6,-6.4)$) --
    ($(current page.north west)+(11.0,-6.4)$);

  %% Sottotitolo
  \node[black, font=\Large\itshape, rotate=-7, anchor=west]
    at ($(current page.north west)+(1.6,-7.1)$)
    {appunti su Martin-L\"of Type Theory};

  %% =========================================================
  %% REGOLA DI INFERENZA FRANTUMATA
  %% Premesse su piani diversi, barra a schegge, conclusione con scia
  %% =========================================================

  %% --- Premesse, ciascuna su un piano differente ---

  %% Piano lontano: più piccola, ruotata molto, sfumata
  \node[black!75, font=\Large\bfseries\sffamily, rotate=-18, opacity=0.85]
    at ($(current page.north west)+(3.2,-10.7)$)
    {$A : \mathsf{Type}$};

  %% Piano centrale: vinaccia, dimensione media, rotazione opposta
  \node[halfgray!90!black, font=\huge\bfseries\sffamily, rotate=8]
    at ($(current page.north west)+(9.5,-11.4)$)
    {$x : A$};

  %% Piano vicino: più grande, nero pieno, altra rotazione
  \node[black, font=\Huge\bfseries\sffamily, rotate=-12]
    at ($(current page.north west)+(15.2,-10.2)$)
    {$x \vdash B\,x$};

  %% --- BARRA A SCHEGGE: tre segmenti disgiunti, spessori e angoli diversi ---

  %% Segmento sinistro, spesso
  \draw[black, line width=7pt, line cap=butt]
    ($(current page.north west)+(1.2,-13.5)$) --
    ($(current page.north west)+(7.6,-13.7)$);

  %% Segmento centrale, più sottile, inclinato di più
  \draw[black, line width=6pt, line cap=butt]
    ($(current page.north west)+(8.2,-13.3)$) --
    ($(current page.north west)+(13.0,-13.9)$);

  %% Segmento destro, il più spesso
  \draw[black, line width=8pt, line cap=butt]
    ($(current page.north west)+(13.7,-13.8)$) --
    ($(current page.north west)+(19.5,-14.1)$);

  %% --- Schegge triangolari sparse intorno alla barra ---

  \fill[halfgray]
    ($(current page.north west)+(7.7,-13.55)$) --
    ($(current page.north west)+(8.1,-13.4)$) --
    ($(current page.north west)+(7.9,-12.8)$) -- cycle;

  \fill[black]
    ($(current page.north west)+(13.15,-13.7)$) --
    ($(current page.north west)+(13.6,-13.85)$) --
    ($(current page.north west)+(13.4,-14.55)$) -- cycle;

  \fill[halfgray, opacity=0.7]
    ($(current page.north west)+(4.8,-13.55)$) --
    ($(current page.north west)+(5.3,-13.7)$) --
    ($(current page.north west)+(5.0,-12.85)$) -- cycle;

  \fill[black, opacity=0.6]
    ($(current page.north west)+(16.2,-13.95)$) --
    ($(current page.north west)+(16.7,-14.0)$) --
    ($(current page.north west)+(16.4,-14.7)$) -- cycle;

  \fill[terra, opacity=0.75]
    ($(current page.north west)+(2.5,-13.5)$) --
    ($(current page.north west)+(3.0,-13.6)$) --
    ($(current page.north west)+(2.7,-12.95)$) -- cycle;

  \fill[halfgray, opacity=0.6]
    ($(current page.north west)+(11.0,-13.6)$) --
    ($(current page.north west)+(11.4,-13.55)$) --
    ($(current page.north west)+(11.2,-12.95)$) -- cycle;

  %% Schegge a forma di linea (frammenti volanti)
  \draw[halfgray, line width=1.4pt]
    ($(current page.north west)+(5.6,-12.7)$) -- ++(75:0.8cm);
  \draw[black, line width=1.2pt, opacity=0.75]
    ($(current page.north west)+(10.4,-14.5)$) -- ++(-70:0.7cm);
  \draw[terra, line width=1.2pt]
    ($(current page.north west)+(17.0,-13.3)$) -- ++(60:0.6cm);

  %% --- CONCLUSIONE con scia Boccioni: echi in discesa diagonale ---

  %% Echi posteriori, opacità decrescente, rotazione crescente (swirl)
  \foreach \dx/\dy/\rot/\op in {%
    0.20/-0.50/-7/0.45,
    0.50/-1.05/-8/0.25,
    0.85/-1.65/-9/0.15,
    1.25/-2.30/-10/0.08%
  } {
    \node[black, font=\huge\bfseries\sffamily, rotate=\rot, opacity=\op]
      at ($(current page.north west)+(9+\dx,-16.5-\dy)$)
      {$(x : A) \to B\,x$};
  }

  %% Conclusione in primo piano
  \node[black, font=\huge\bfseries\sffamily, rotate=-6]
    at ($(current page.north west)+(9,-16.5)$)
    {$(x : A) \to B\,x$};

  %% =========================================================
  %% SIGILLO F·I·E·C --- ruotato e spostato a destra
  %% =========================================================
  \node[halfgray, font=\bfseries\scshape\Large, rotate=5]
    at ($(current page.north west)+(13.5,-21.5)$)
    {F \;$\cdot$\; I \;$\cdot$\; E \;$\cdot$\; C};

  %% =========================================================
  %% FOOTER --- asimmetrico, in basso-sinistra, leggermente ruotato
  %% =========================================================
  \node[black, font=\itshape\large, anchor=west, rotate=-2]
    at ($(current page.south west)+(1.2,3.0)$)
    {compendio a \emph{Verified Functional Programming in Agda}};
  \node[halfgray, font=\normalsize, anchor=west, rotate=-2]
    at ($(current page.south west)+(1.2,2.2)$)
    {Aaron Stump \;\textbullet\; Iowa Agda Library};
  \node[terra, font=\small\bfseries\scshape, anchor=west, rotate=-2]
    at ($(current page.south west)+(1.2,1.5)$)
    {\the\year};

\end{tikzpicture}
\end{titlepage}
\clearpage

% ----------------- nota al lettore -----------------
\chapter*{Nota al lettore}
\addcontentsline{toc}{chapter}{Nota al lettore}

\begin{center}
{\large\bfseries\scshape\color{halfgray}
Nessuna magia.\\[2pt]
Nessun trucco.\\[2pt]
Il tipo esiste o non esiste.\\[2pt]
La prova regge o crolla.\\[2pt]
Chi non dimostra, tace.}
\end{center}

\bigskip\bigskip

Quattro cose, prima di cominciare.

\paragraph{Origine.} Queste note nascono per accompagnare la lettura di \emph{Verified Functional Programming in Agda} di Aaron Stump, leggendo gli esercizi come applicazioni della Martin-L\"of Type Theory pura --- cio\`e nello schema delle quattro regole F-I-E-C (Formazione, Introduzione, Eliminazione, Computazione) che sotto Agda c'\`e sempre, anche quando la sintassi la nasconde dietro lo zucchero.

\paragraph{Tesi.} Tutto il gioco di MLTT \`e in quattro regole, applicate uniformemente a ogni tipo. Capire ciascun tipo significa scriverne le quattro regole e vedere come gli abitanti del linguaggio (termini, prove) vengono dalle regole di introduzione, e come gli usi (eliminazioni, riduzioni) vengono da quelle di eliminazione e computazione. Non c'\`e altro.

\paragraph{Assunzioni.} Sono dichiarate ed esplicite. Le ho chiamate \emph{ceri} --- come i ceri votivi che si accendono perch\'e qualcosa regga senza averne la dimostrazione interna --- e ne incontreremo quattro nel corpo del testo. Ognuno \`e un punto in cui il sistema poggia su qualcosa che non si pu\`o derivare dall'interno: vivono nel metalinguaggio o sono scelte fondative. Confondere un cero con un teorema, o uno zucchero sintattico con una regola primitiva, \`e la fonte principale di confusione in MLTT. Renderli espliciti \`e met\`a del lavoro.

\paragraph{Forma.} Dove la prosa basta, c'\`e prosa. Dove la prosa rende sfuocato qualcosa che chiede precisione, entrano le regole di inferenza in forma classica (premesse, barra, conclusione), o un frammento Agda. Nessun salto: ogni sezione usa solo cose introdotte in sezioni precedenti, e se ci si perde su un punto basta tornare indietro fino a trovarne il fondamento.

\bigskip

\noindent\textbf{Patto di lettura.} Niente salti in avanti. La Parte~I fissa il vocabolario (giudizi, metalinguaggio, grammatica di contesti e $\vdash$, schema F-I-E-C). La Parte~II applica F-I-E-C ai cinque tipi MLTT puri: $\mathbb{B}$, $\mathbb{N}$, $\Pi$, $\Sigma$, il tipo identit\`a $\_\identita\_$. La Parte~III estrae da queste regole la logica intuizionista che vi affiora dietro (Curry-Howard) e un kit di costruzioni derivate (\emph{tipi notevoli}). La Parte~IV \`e il compendio capitolo per capitolo di \emph{Verified Functional Programming in Agda}: vi atterrano fra l'altro l'esercizio paradigmatico di commutativit\`a di \texttt{\&\&} (Stump cap.~1) e il tipo dipendente $\mathrm{Vec}$ (Stump cap.~4). Ceri e decisioni di design sono raccolti nell'appendice (\S\ref{cap:ceri}).

\bigskip

\noindent\textbf{Strumenti.} Stile tipografico Pantieri (\textsc{ClassicThesis} + \textsc{ArsClassica}); ambienti teorema \textsc{regola} (numerate per capitolo), \textsc{definizione} (sullo stesso contatore), \textsc{osservazione} (senza numero), \textsc{cero} (numerato a parte, fuori dai capitoli, per tenere le assunzioni globali leggibili come un indice). Il codice Agda \`e reso con \texttt{listings} e mappa Unicode --- quindi compila con \texttt{pdflatex} senza shell-escape.

\clearpage

% ----------------- indice -----------------
{\setlength{\parskip}{0pt}\tableofcontents}
\clearpage

\pagenumbering{arabic}
\pagestyle{scrheadings}

% ----------------- corpo del libro -----------------
\input{parte-i-fondamenta}
\input{parte-ii-tipi}
\input{parte-iii-prove}
\input{parte-iv-compendio-stump}

\appendix
\input{parte-iv-indice}

\end{document}
