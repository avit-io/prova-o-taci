\documentclass[
  oneside,
  openany,
  titlepage,
  11pt,
  a4paper,
  italian,
  DIV=14,
  headinclude,
  footinclude=true,
  cleardoublepage=empty
]{scrbook}

% --- ClassicThesis + ArsClassica (Pantieri) ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{babel}
\usepackage{lmodern}
\usepackage{mathpazo}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{mathrsfs}
\usepackage[
  eulerchapternumbers,
  beramono,
  eulermath,
  pdfspacing,
  parts
]{classicthesis}
\usepackage{arsclassica}

% --- nota di colore vinaccia per il numero del capitolo ---
\definecolor{halfgray}{rgb}{0.55,0.13,0.16}
\definecolor{cream}{RGB}{245,240,228}
\definecolor{terra}{rgb}{0.42,0.26,0.13}  % terra d'ombra calda, per stratificazioni semi-trasparenti

% --- aumenta lo spazio dopo il titolo del capitolo ---
\usepackage{titlesec}
\titlespacing*{\chapter}{0pt}{2.5\baselineskip}{4\baselineskip}

% --- lingua e tolleranze ---
\selectlanguage{italian}
\emergencystretch=3em
\tolerance=2000
\hbadness=2000

% --- grafica ---
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,decorations.pathmorphing,calc}

% --- epigrafi (per attacchi capitolo, opzionale) ---
\usepackage{epigraph}
\setlength{\epigraphwidth}{0.55\textwidth}
\renewcommand{\epigraphflush}{flushright}
\renewcommand{\textflush}{flushright}
\renewcommand{\epigraphsize}{\small\itshape}

% --- regole di inferenza in stile naturale ---
\usepackage{mathpartir}

% --- codice Agda con listings (no shell-escape richiesto) ---
\usepackage{listings}
\usepackage{xcolor}
\definecolor{agdaKw}{rgb}{0.55,0.13,0.16}   % maroon, come halfgray
\definecolor{agdaCom}{rgb}{0.45,0.45,0.45}
\definecolor{agdaStr}{rgb}{0.20,0.40,0.20}

% --- mapping unicode per Agda (così pdflatex digerisce i simboli) ---
\lstdefinelanguage{Agda}{
  keywords={data, where, with, module, open, import, public, private,
            renaming, hiding, using, let, in, do, postulate, abstract,
            primitive, mutual, infix, infixl, infixr, syntax, pattern,
            instance, record, field, constructor, Set, Type, Prop,
            forall, λ, lambda},
  sensitive=true,
  comment=[l]{--},
  morecomment=[s]{\{-}{-\}},
  string=[b]",
  basicstyle=\ttfamily\small,
  keywordstyle=\color{agdaKw}\bfseries,
  commentstyle=\color{agdaCom}\itshape,
  stringstyle=\color{agdaStr},
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  literate=
    {𝔹}{{$\mathbb{B}$}}1
    {ℕ}{{$\mathbb{N}$}}1
    {≡}{{$\equiv$}}1
    {∀}{{$\forall$}}1
    {→}{{$\rightarrow$}}1
    {⊢}{{$\vdash$}}1
    {⊥}{{$\bot$}}1
    {⊤}{{$\top$}}1
    {⟦}{{$\llbracket$}}1
    {⟧}{{$\rrbracket$}}1
    {λ}{{$\lambda$}}1
    {Π}{{$\Pi$}}1
    {Σ}{{$\Sigma$}}1
    {α}{{$\alpha$}}1
    {β}{{$\beta$}}1
    {δ}{{$\delta$}}1
    {ε}{{$\varepsilon$}}1
    {ρ}{{$\rho$}}1
    {τ}{{$\tau$}}1
    {φ}{{$\varphi$}}1
    {₁}{{$_1$}}1
    {₂}{{$_2$}}1
    {⟨}{{$\langle$}}1
    {⟩}{{$\rangle$}}1
    {≢}{{$\not\equiv$}}1
    {∎}{{$\blacksquare$}}1
    {∷}{{$::$}}1
    {↑}{{$\uparrow$}}1
    {↓}{{$\downarrow$}}1
    {ℝ}{{$\mathbb{R}$}}1
    {ℤ}{{$\mathbb{Z}$}}1
    {⊎}{{$\uplus$}}1
    {★}{{$\star$}}1
    {∧}{{$\wedge$}}1
    {∨}{{$\vee$}}1
    {¬}{{$\neg$}}1
    {∃}{{$\exists$}}1
    {∘}{{$\circ$}}1
    {×}{{$\times$}}1
    {è}{{\`e}}1
    {à}{{\`a}}1
    {ò}{{\`o}}1
    {ì}{{\`\i }}1
    {ù}{{\`u}}1
    {é}{{\'e}}1
    {È}{{\`E}}1
    {À}{{\`A}}1
    {Ì}{{\`I}}1
    {Ò}{{\`O}}1
    {Ù}{{\`U}}1
    {É}{{\'E}}1,
}

\lstnewenvironment{agda}[1][]{\lstset{language=Agda,#1}}{}
\newcommand{\agdainl}[1]{\lstinline[language=Agda,basicstyle=\ttfamily\normalsize]{#1}}

% --- ambienti teorema-like in stile ClassicThesis (identici al sorgente avit-io) ---
\newtheoremstyle{ctplain}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\itshape}{}%
  {\bfseries\scshape}{.}%
  {.5em}{}%
\newtheoremstyle{ctdef}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\normalfont}{}%
  {\bfseries\scshape}{.}%
  {.5em}{}%
\newtheoremstyle{ctrem}%
  {0.7\baselineskip}{0.5\baselineskip}%
  {\normalfont}{}%
  {\itshape}{.}%
  {.5em}{}%

% --- ambiente per i ceri: stesso stile sobrio dello stile def, ma con head dedicato ---
\newtheoremstyle{ctcero}%
  {0.9\baselineskip}{0.7\baselineskip}%
  {\normalfont}{}%
  {\bfseries\scshape\color{halfgray}}{.}%
  {.5em}{}%

\theoremstyle{ctplain}
\newtheorem{regola}{Regola}[chapter]

\theoremstyle{ctdef}
\newtheorem{defn}[regola]{Definizione}

\theoremstyle{ctrem}
\newtheorem*{oss}{Osservazione}

\theoremstyle{ctcero}
\newtheorem{cero}{Cero}

% --- comandi locali ---
\newcommand{\meta}[1]{\textit{#1}}        % per il metalinguaggio in prosa
\newcommand{\lang}[1]{\textbf{#1}}        % per il linguaggio in prosa
\newcommand{\definitorio}{\ensuremath{\equiv_{\beta}}}
\newcommand{\identita}{\ensuremath{\equiv}}
\newcommand{\Type}{\mathsf{Type}}
\newcommand{\Set}{\mathsf{Set}}
\newcommand{\indB}{\mathsf{ind\text{-}\mathbb{B}}}
\newcommand{\indN}{\mathsf{ind\text{-}\mathbb{N}}}
\newcommand{\indS}{\mathsf{ind\text{-}\Sigma}}
\newcommand{\indV}{\mathsf{ind\text{-}Vec}}
\newcommand{\Jelim}{\mathsf{J}}
\newcommand{\refl}{\mathsf{refl}}
\newcommand{\zero}{\mathsf{zero}}
\newcommand{\suc}{\mathsf{suc}}
\newcommand{\tto}{\mathsf{tt}}
\newcommand{\ff}{\mathsf{ff}}
\newcommand{\nil}{\mathsf{nil}}
\newcommand{\cons}{\mathsf{cons}}
\newcommand{\andd}{\mathbin{\&\&}}

% --- hyperref ---
\hypersetup{%
  colorlinks=true,
  linkcolor=Maroon,
  citecolor=Maroon,
  urlcolor=Maroon,
  pdftitle={Prova o taci},
  pdfsubject={appunti di MLTT per Verified Functional Programming in Agda},
  pdfauthor={}
}

% --- TOC ---
\usepackage{titletoc}
\renewcommand{\cftpartfont}{\color{CTtitle}}
\renewcommand{\cftpartaftersnum}{}
\renewcommand{\cftpartaftersnumb}{}
\renewcommand{\cftchapfont}{}
\renewcommand{\cftchappresnum}{}
\renewcommand{\cftchapaftersnumb}{}

\begin{document}
\frenchspacing
\raggedbottom
\selectlanguage{italian}
\renewcommand{\partname}{Parte}
\renewcommand{\chaptername}{Capitolo}
\renewcommand{\contentsname}{Indice}

\pagenumbering{roman}
\pagestyle{plain}

% ----------------- COPERTINA -----------------
% L'IDEA: la copertina E' una regola di inferenza.
% Premesse F, I, E, C in alto. Barra inferenziale che taglia la pagina.
% Conclusione: il titolo, sotto. Il libro deriva il proprio nome.
% Top-down: dall'alto le quattro regole, in basso quello che ne segue.
\begin{titlepage}
\thispagestyle{empty}
\begin{tikzpicture}[remember picture, overlay]

  %% Sfondo cream pulito. Niente decorazione.
  \fill[cream] (current page.north west) rectangle (current page.south east);

  %% =========================================================
  %% PREMESSE --- in alto, quattro giudizi affiancati.
  %% Ciascuno: una lettera grande in vinaccia + il giudizio formale
  %% sotto, in mono. Sono i quattro pilastri F-I-E-C.
  %% =========================================================

  %% F --- formazione
  \node[halfgray, anchor=base,
        font=\fontsize{56}{60}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(3.5,-6.0)$) {F};
  \node[black, anchor=base,
        font=\small\ttfamily]
    at ($(current page.north west)+(3.5,-7.0)$) {$A : \mathsf{Type}$};

  %% I --- introduzione
  \node[halfgray, anchor=base,
        font=\fontsize{56}{60}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(7.5,-6.0)$) {I};
  \node[black, anchor=base,
        font=\small\ttfamily]
    at ($(current page.north west)+(7.5,-7.0)$) {$a : A$};

  %% E --- eliminazione
  \node[halfgray, anchor=base,
        font=\fontsize{56}{60}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(12.0,-6.0)$) {E};
  \node[black, anchor=base,
        font=\small\ttfamily]
    at ($(current page.north west)+(12.0,-7.0)$) {$a : A \vdash b : B$};

  %% C --- computazione
  \node[halfgray, anchor=base,
        font=\fontsize{56}{60}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(17.0,-6.0)$) {C};
  \node[black, anchor=base,
        font=\small\ttfamily]
    at ($(current page.north west)+(17.0,-7.0)$) {$b \equiv_{\beta} b'$};

  %% =========================================================
  %% BARRA INFERENZIALE --- IL GESTO della copertina.
  %% Spessa, nera, attraversa l'intera pagina da margine a margine.
  %% Questa barra dice: sopra ci sono le premesse, sotto la conclusione.
  %% =========================================================
  \fill[black]
    ($(current page.north west)+(1.5,-9.4)$) rectangle
    ($(current page.north east)+(-1.5,-10.1)$);

  %% Etichetta a destra della barra, in vinaccia: "FIEC" come nome della
  %% regola d'inferenza (come si fa nelle derivazioni formali).
  \node[halfgray, anchor=west,
        font=\small\sffamily\bfseries]
    at ($(current page.north east)+(-1.3,-9.75)$) {};
  \node[halfgray, anchor=east,
        font=\footnotesize\sffamily\itshape]
    at ($(current page.north west)+(20.0,-10.6)$)
    {(\textsc{fiec})};

  %% =========================================================
  %% CONCLUSIONE --- il titolo. Tre parole su tre righe,
  %% allineate a sinistra, sotto la barra. PROVA in nero pieno,
  %% O in italic vinaccia (la disgiunzione che brucia),
  %% TACI in nero su fondo. Una sola colonna, perentoria.
  %% =========================================================
  \node[black, anchor=base west,
        font=\fontsize{96}{100}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(2.0,-15.2)$)
    {PROVA};

  \node[halfgray, anchor=base west,
        font=\fontsize{96}{100}\selectfont\sffamily\bfseries\itshape]
    at ($(current page.north west)+(2.0,-18.6)$)
    {O};

  \node[black, anchor=base west,
        font=\fontsize{96}{100}\selectfont\sffamily\bfseries]
    at ($(current page.north west)+(2.0,-22.0)$)
    {TACI};

  %% Sottile filo vinaccia che corre lungo il fianco sinistro
  %% del titolo --- segna "questa colonna E' la conclusione".
  \fill[halfgray]
    ($(current page.north west)+(1.6,-22.4)$) rectangle
    ($(current page.north west)+(1.8,-12.5)$);

  %% =========================================================
  %% FOOTER --- piede di pagina, sottile, metadata.
  %% =========================================================

  %% Linea sottile orizzontale, asciutta
  \fill[black]
    ($(current page.south west)+(2.0,3.6)$) rectangle
    ($(current page.south east)+(-2.0,3.65)$);

  %% Sottotitolo a sinistra
  \node[black, anchor=base west,
        font=\normalsize\sffamily\bfseries]
    at ($(current page.south west)+(2.0,2.6)$)
    {\spacedlowsmallcaps{appunti di Martin-L\"of Type Theory}};

  %% Compendio a Stump
  \node[terra, anchor=base west,
        font=\footnotesize\sffamily]
    at ($(current page.south west)+(2.0,1.8)$)
    {\spacedlowsmallcaps{compendio a Verified Functional Programming in Agda}};

  %% Anno a destra
  \node[halfgray, anchor=base east,
        font=\large\sffamily\bfseries]
    at ($(current page.south east)+(-2.0,2.6)$)
    {MMXXVI};

\end{tikzpicture}
\end{titlepage}
\clearpage

% ----------------- MANIFESTO (omaggio a Marinetti) -----------------
% Pagina autonoma. Numerata 1..9. Forma anaforica, perentoria.
% Contenuto: MLTT come matematica del processo.
\thispagestyle{empty}
\vspace*{2cm}
\begin{center}
{\sffamily\bfseries\fontsize{30}{34}\selectfont\color{halfgray}
\spacedlowsmallcaps{Manifesto}}\\[6pt]
{\itshape\small della matematica del processo}
\end{center}
\vspace{1.2cm}

\begingroup
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55\baselineskip}
\leftskip=2em \rightskip=2em

{\color{halfgray}\sffamily\bfseries 1.}\quad
Noi dichiariamo che un tipo \emph{esiste} solo se le sue regole lo dicono. Niente esiste fuori da come si forma, si introduce, si elimina, si computa.

{\color{halfgray}\sffamily\bfseries 2.}\quad
Noi rifiutiamo il terzo escluso come premessa universale. La verit\`a non \`e un giudizio che precede la prova: la prova \`e la verit\`a, e dove la prova manca conviene tacere.

{\color{halfgray}\sffamily\bfseries 3.}\quad
Noi affermiamo il \emph{processo} contro la stasi. Una funzione non \`e un grafo immobile: \`e una computazione che riduce, normalizza, produce un valore canonico in un numero finito di passi.

{\color{halfgray}\sffamily\bfseries 4.}\quad
Noi proclamiamo che si pu\`o partire dall'alto. Si sceglie il livello di astrazione del processo; le cose elementari si fanno \emph{accadere}, e il \emph{pattern matching} le riconosce nel momento in cui accadono.

{\color{halfgray}\sffamily\bfseries 5.}\quad
Noi vogliamo quattro regole sole: \textbf{Formazione, Introduzione, Eliminazione, Computazione}. Le applichiamo uniformi a ogni tipo. Tutto il resto \`e zucchero, e lo zucchero non \`e una regola.

{\color{halfgray}\sffamily\bfseries 6.}\quad
Noi diciamo \emph{ceri} le assunzioni che reggono il sistema senza prova interna. Le contiamo. Le teniamo in vista. Confondere un cero con un teorema \`e la prima fonte di confusione, e noi la combattiamo.

{\color{halfgray}\sffamily\bfseries 7.}\quad
Noi opponiamo la \emph{geometria del derivare} alla retorica del dimostrato. Una regola \`e premesse sopra una barra, una conclusione sotto. Tutto qui. Tutto necessario.

{\color{halfgray}\sffamily\bfseries 8.}\quad
Noi sosteniamo che un programma \emph{\`e} una prova, e una prova \emph{\`e} un programma. Curry-Howard non \`e analogia: \`e identit\`a. La logica affiora dalla struttura dei tipi --- non viene importata.

{\color{halfgray}\sffamily\bfseries 9.}\quad
Noi vogliamo costruire, non descrivere. Dove la classica racconta cosa \emph{c'\`e}, noi diciamo cosa si \emph{fa}. La matematica del processo si scrive col verbo all'imperativo.

\endgroup

\vfill
\begin{center}
{\sffamily\bfseries\color{halfgray}\spacedlowsmallcaps{Prova o taci}}
\end{center}
\clearpage

% ----------------- frontespizio (tipografico, asimmetrico) -----------------
\begin{titlepage}
\thispagestyle{empty}
\begin{tikzpicture}[remember picture, overlay]

  %% Sfondo: cream uniforme. Niente blocchi, niente bande.
  \fill[cream] (current page.north west) rectangle (current page.south east);

  %% =========================================================
  %% Blocco TITOLO --- flush left, terzo alto.
  %% Peso visivo: alto. Anchor compositivo top-left.
  %% =========================================================

  %% Titolo: spaced lowsmallcaps, vinaccia
  \node[halfgray, anchor=base west,
        font=\fontsize{38}{42}\selectfont\scshape]
    at ($(current page.north west)+(4.5,-9)$)
    {\spacedlowsmallcaps{Prova o taci}};

  %% Filo vinaccia sotto il titolo
  \draw[halfgray, line width=0.6pt]
    ($(current page.north west)+(4.5,-9.7)$) --
    ($(current page.north west)+(13.5,-9.7)$);

  %% Sottotitolo: italic, nero, sotto la rule.
  %% Tre righe: una piccola di servizio, due grandi che dominano il terzo alto.
  \node[black, anchor=base west,
        font=\large\itshape]
    at ($(current page.north west)+(4.5,-10.4)$)
    {appunti su};

  \node[black, anchor=base west,
        font=\fontsize{26}{30}\selectfont\itshape]
    at ($(current page.north west)+(4.5,-11.6)$)
    {Martin-L\"of};

  \node[black, anchor=base west,
        font=\fontsize{26}{30}\selectfont\itshape]
    at ($(current page.north west)+(4.5,-13.0)$)
    {Type Theory};

  %% =========================================================
  %% Emblema F·I·E·C --- flush right, centro verticale.
  %% Pivot della composizione: colonna verticale dei quattro
  %% nomi espansi, lettera iniziale grande in vinaccia, resto
  %% in italic nero. Due fili sottili come bracket tipografico.
  %% =========================================================

  \draw[halfgray, line width=0.4pt]
    ($(current page.north east)+(-9.0,-15.4)$) --
    ($(current page.north east)+(-4.5,-15.4)$);

  %% Per allineare le lettere F·I·E·C in colonna a destra, usiamo
  %% un tabular con prima colonna right-aligned (lettera) e
  %% seconda colonna left-aligned (suffisso italic), ancorando
  %% l'intero blocco al margine destro.
  \node[anchor=north east]
    at ($(current page.north east)+(-4.5,-15.9)$)
    {\renewcommand{\arraystretch}{1.35}%
     \begin{tabular}{@{}r@{\,}l@{}}
       {\color{halfgray}\fontsize{22}{26}\selectfont\scshape F} &
         {\fontsize{14}{18}\selectfont\itshape ormazione}\\
       {\color{halfgray}\fontsize{22}{26}\selectfont\scshape I} &
         {\fontsize{14}{18}\selectfont\itshape ntroduzione}\\
       {\color{halfgray}\fontsize{22}{26}\selectfont\scshape E} &
         {\fontsize{14}{18}\selectfont\itshape liminazione}\\
       {\color{halfgray}\fontsize{22}{26}\selectfont\scshape C} &
         {\fontsize{14}{18}\selectfont\itshape omputazione}\\
     \end{tabular}};

  \draw[halfgray, line width=0.4pt]
    ($(current page.north east)+(-9.0,-20.6)$) --
    ($(current page.north east)+(-4.5,-20.6)$);

  %% =========================================================
  %% Blocco FOOTER --- flush left, terzo basso.
  %% Peso visivo: basso. Tre righe + filo corto.
  %% =========================================================

  %% Filo corto sopra il footer
  \draw[halfgray, line width=0.4pt]
    ($(current page.south west)+(4.5,7.6)$) --
    ($(current page.south west)+(7.5,7.6)$);

  %% Autore
  \node[black, anchor=base west,
        font=\large]
    at ($(current page.south west)+(4.5,6.8)$)
    {Aaron Stump};

  %% Compendio info, italic
  \node[black, anchor=base west,
        font=\normalsize\itshape]
    at ($(current page.south west)+(4.5,6.0)$)
    {compendio a \emph{Verified Functional Programming in Agda}};

  %% Affiliazione
  \node[black, anchor=base west,
        font=\normalsize]
    at ($(current page.south west)+(4.5,5.2)$)
    {Iowa Agda Library};

  %% =========================================================
  %% Anno --- flush right basso. Contrappeso a F·I·E·C.
  %% =========================================================

  \node[halfgray, anchor=base east,
        font=\large\bfseries\scshape]
    at ($(current page.south east)+(-4.5,3.0)$)
    {MMXXVI};

\end{tikzpicture}
\end{titlepage}
\clearpage

% ----------------- nota al lettore -----------------
\chapter*{Nota al lettore}
\addcontentsline{toc}{chapter}{Nota al lettore}

\begin{center}
{\itshape\color{halfgray}
Un tipo esiste se le sue regole lo dichiarano; una prova vale se i suoi passi reggono.\\[2pt]
Dove non si dimostra, conviene tacere --- da qui il titolo.}
\end{center}

\bigskip\bigskip

Quattro cose, prima di cominciare.

\paragraph{Origine.} Queste note nascono per accompagnare la lettura di \emph{Verified Functional Programming in Agda} di Aaron Stump, leggendo gli esercizi come applicazioni di Martin-L\"of Type Theory pura --- cio\`e nello schema F-I-E-C (Formazione, Introduzione, Eliminazione, Computazione) che sotto Agda c'\`e sempre, anche quando lo zucchero sintattico lo nasconde.

\paragraph{Tesi.} Tutto MLTT sta in quattro regole, applicate uniformemente a ogni tipo. Capire un tipo significa scriverne F-I-E-C e vedere come gli abitanti vengono dalle regole I, e come gli usi vengono da E e C. Non c'\`e altro.

\paragraph{Assunzioni.} Sono dichiarate ed esplicite. Le chiamiamo \emph{ceri} --- come i ceri votivi che si accendono perch\'e qualcosa regga senza dimostrazione interna --- e ne incontreremo quattro. Ognuno \`e un punto in cui il sistema poggia su qualcosa di non derivabile dall'interno: appartiene al metalinguaggio, oppure \`e una scelta di design. Confondere un cero con un teorema, o uno zucchero sintattico con una regola primitiva, \`e la fonte principale di confusione in MLTT; tenerli espliciti \`e met\`a del lavoro.

\paragraph{Forma.} Dove la prosa basta, c'\`e prosa. Dove la prosa rende sfuocato qualcosa che chiede precisione, entrano le regole di inferenza in forma classica (premesse, barra, conclusione), o un frammento Agda. Nessun salto: ogni sezione usa solo cose introdotte in sezioni precedenti, e se ci si perde basta tornare indietro fino a trovarne il fondamento.

\bigskip

\noindent\textbf{Patto di lettura.} Niente salti in avanti. La Parte~I fissa il vocabolario (giudizi, metalinguaggio, grammatica di contesti e $\vdash$, schema F-I-E-C). La Parte~II applica F-I-E-C ai sei tipi MLTT puri --- $\mathbb{B}$, $\mathbb{N}$, $\Pi$, $\Sigma$, $A \uplus B$, il tipo identit\`a $\_\identita\_$ --- pi\`u un esempio dipendente piccolo ($\mathrm{Fin}\,n$). La Parte~III estrae da queste regole la logica intuizionista che vi affiora dietro (Curry-Howard) e un kit di costruzioni derivate (\emph{tipi notevoli}). La Parte~IV \`e il compendio capitolo per capitolo di \emph{Verified Functional Programming in Agda}: vi atterrano fra l'altro l'esercizio paradigmatico di commutativit\`a di \texttt{\&\&} (Stump cap.~1) e il tipo dipendente $\mathrm{Vec}$ (Stump cap.~4). Ceri e decisioni di design sono raccolti nell'appendice (\S\ref{cap:ceri}).

\medskip

\noindent\textbf{Convenzione sul contesto.} Da \S\ref{cap:fiec} in poi le regole sono scritte \emph{senza} contesto in vista: si sottintende un $\Gamma$ implicito (per esteso, ogni giudizio \`e $\Gamma \vdash \dots$ per ogni $\Gamma$). Il $\vdash$ ricompare solo quando una premessa \emph{estende} il contesto rispetto alla conclusione --- caso paradigmatico, la $\Pi$-formazione (\S\ref{cap:pi}). Lo si dichiara una volta qui, e una sola; il dettaglio operativo \`e in \S\ref{cap:contesti}.

\bigskip

\noindent\textbf{Strumenti.} Stile tipografico Pantieri (\textsc{ClassicThesis} + \textsc{ArsClassica}); ambienti teorema \textsc{regola} (numerate per capitolo), \textsc{definizione} (sullo stesso contatore), \textsc{osservazione} (senza numero), \textsc{cero} (numerato a parte, fuori dai capitoli, per tenere le assunzioni globali leggibili come un indice). Il codice Agda \`e reso con \texttt{listings} e mappa Unicode --- quindi compila con \texttt{pdflatex} senza shell-escape.

\clearpage

% ----------------- indice -----------------
{\setlength{\parskip}{0pt}\tableofcontents}
\clearpage

\pagenumbering{arabic}
\pagestyle{scrheadings}

% ----------------- corpo del libro -----------------
\input{parte-i-fondamenta}
\input{parte-ii-tipi}
\input{parte-iii-prove}
\input{parte-iv-compendio-stump}

\appendix
\input{parte-iv-indice}
\input{appendice-contesti}

\end{document}
